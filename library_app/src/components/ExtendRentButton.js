import React from 'react';

const ExtendRentButton = ({ onClick }) => {
  return (
    <button className="extend-rent-button" onClick={onClick}>Extend Rent</button>
  );
};

export default ExtendRentButton;